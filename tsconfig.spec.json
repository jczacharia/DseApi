/* Angular unit tests (Vitest via @angular/build:unit-test). */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": ["vitest/globals"]
  },
  "include": ["ui/**/*.spec.ts"]
}
